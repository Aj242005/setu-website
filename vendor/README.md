# Three.js

Three.js 0.186.0, downloaded from the npm `three` package. The two unmodified
ES modules are self-hosted so the scene does not depend on a runtime CDN.
`LICENSE` is the upstream MIT license. Only the WebGL renderer is used.
